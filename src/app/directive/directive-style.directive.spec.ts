import { DirectiveStyleDirective } from './directive-style.directive';

describe('DirectiveStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveStyleDirective();
    expect(directive).toBeTruthy();
  });
});
